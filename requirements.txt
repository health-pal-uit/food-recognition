# Minimal requirements for YOLOv8 detection API on Heroku
# Target: <300MB slug size
--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
Flask>=2.0.0
blinker>=1.6.0
gunicorn==20.1.0
flask-cors==6.0.2

# Core ML/CV (CPU-only)
torch==2.2.2+cpu
torchvision==0.17.2+cpu
opencv-python-headless==4.7.0.72
numpy==1.26.4
Pillow>=9.0.0

# YOLOv8 dependencies
pyyaml>=5.1
omegaconf
scipy
ensemble-boxes
matplotlib
cycler

# Additional utilities
pandas
tqdm
tabulate

# ML model dependencies
timm
segmentation-models-pytorch
huggingface-hub

# Cloud model loading
gdown>=4.4.0

# Nutrition API
requests>=2.28.0

# Image augmentation (pinned version for compatibility)
albumentations==1.1.0